import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient(); async function deleteOldResources() { try { console.log("🔄 Eliminando recursos antiguos..."); const oldSlugs = ["tutorial-machine-learning", "datasets-para-ia", "herramientas-desarrollo-ia", "papers-deep-learning"]; for (const slug of oldSlugs) { const deleted = await prisma.resource.deleteMany({ where: { slug } }); console.log(`❌ Eliminado ${deleted.count} recurso(s) con slug: ${slug}`); } console.log("✅ Recursos antiguos eliminados"); } catch (error) { console.error("Error:", error); } finally { await prisma.$disconnect(); } } deleteOldResources();
