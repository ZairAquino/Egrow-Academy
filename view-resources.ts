import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient(); async function viewResources() { try { await prisma.$connect(); const resources = await prisma.resource.findMany({ select: { slug: true, title: true, description: true, shortDescription: true, imageUrl: true } }); console.log("📋 Recursos actuales:"); resources.forEach(r => { console.log(`\n${r.title} (${r.slug})`); console.log(`Desc: ${r.description}`); console.log(`Short: ${r.shortDescription}`); console.log(`Image: ${r.imageUrl || "Sin imagen"}`); }); } catch (error) { console.error("Error:", error); } finally { await prisma.$disconnect(); } } viewResources();
