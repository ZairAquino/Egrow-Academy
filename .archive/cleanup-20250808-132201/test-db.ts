import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient(); async function testConnection() { try { console.log("🔄 Probando conexión..."); await prisma.$connect(); console.log("✅ Conexión OK"); const count = await prisma.resource.count(); console.log(`📊 Recursos: ${count}`); } catch (error) { console.error("❌ Error:", error); } finally { await prisma.$disconnect(); } } testConnection();
