import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient(); async function testLogin() { try { await prisma.$connect(); const user = await prisma.user.findUnique({ where: { email: "test@example.com" }, select: { email: true, passwordHash: true, isActive: true, emailVerified: true } }); console.log("Usuario test:", user); } catch (error) { console.error("Error:", error); } finally { await prisma.$disconnect(); } } testLogin();
