import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient(); async function verifyUpdates() { try { await prisma.$connect(); const resources = await prisma.resource.findMany({ select: { title: true, description: true, imageUrl: true }, take: 2 }); console.log("📋 Verificación de actualizaciones:"); resources.forEach(r => { console.log(`\n${r.title}`); console.log(`Desc: ${r.description.substring(0, 100)}...`); console.log(`Image: ${r.imageUrl ? "✅ Tiene imagen" : "❌ Sin imagen"}`); }); } catch (error) { console.error("Error:", error); } finally { await prisma.$disconnect(); } } verifyUpdates();
