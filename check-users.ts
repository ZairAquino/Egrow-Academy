import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient(); async function checkUsers() { try { await prisma.$connect(); const users = await prisma.user.findMany({ select: { email: true, isActive: true, emailVerified: true } }); console.log("👥 Usuarios:", users); } catch (error) { console.error("Error:", error); } finally { await prisma.$disconnect(); } } checkUsers();
